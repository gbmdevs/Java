--CREATE TABLE FIXED_EXPENSE_HIST(
--    id VARCHAR(36) NOT NULL PRIMARY KEY,
--    user_id VARCHAR(36) NOT NULL,
--    CONSTRAINT fk_user_oper_fix_exp_hist_id FOREIGN KEY(user_id) REFERENCES usuario(id)
--);


